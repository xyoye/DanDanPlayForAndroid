<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.xyoye.local_component">

    <application
        android:name="com.xyoye.common_component.base.app.BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:targetApi="q">
        <activity android:name=".ui.activities.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.activities.bind_danmu.BindDanmuActivity" />
        <activity android:name=".ui.activities.bind_subtitle.BindSubtitleActivity" />
        <activity android:name=".ui.activities.local_media.LocalMediaActivity" />
        <activity android:name=".ui.activities.play_history.PlayHistoryActivity" />
    </application>

</manifest>